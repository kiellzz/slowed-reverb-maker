{
  "name": "slowed-reverb-backend",
  "version": "1.0.0",
  "description": "Backend for Slowed + Reverb audio processing",
  "main": "dist/server.js",
  "type": "commonjs",
  "scripts": {
    "build": "tsc -p tsconfig.json && tsc -p ../frontend/tsconfig.json",
    "start": "node dist/server.js",
    "dev": "npm run build && node dist/server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "multer": "^1.4.5-lts.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/multer": "^1.4.12",
    "@types/node": "^20.11.30",
    "typescript": "^5.4.5"
  }
}
