services:
  - type: web
    name: slowed-reverb-maker
    runtime: node
    plan: free
    rootDir: backend
    buildCommand: |
      npm ci --include=dev
      npm run build
    startCommand: npm start
